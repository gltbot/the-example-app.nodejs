Jenkinsfile (Declarative Pipeline)
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'cd the-example-app.nodejs'
                sh 'npm --version'
            }
        }
    }
}
